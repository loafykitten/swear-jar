services:
  swear-jar:
    build:
      context: .
      dockerfile: service/Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - service/.env
    volumes:
      - swear-data:/data

volumes:
  swear-data:
