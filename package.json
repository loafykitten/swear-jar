{
	"name": "swear-jar",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"start:service": "cd service && bun start",
		"dev:service": "cd service && bun dev",
		"test:service": "cd service && bun test",
		"start:vox": "cd vox && uv run python src/main.py"
	}
}
